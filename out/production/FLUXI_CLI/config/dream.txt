fluxi make MODEL
fluxi change MODEL remove ...
fluxi change MODEL add ...

fluxi init project PROJECT_NAME phoenix, flutter, vuejs

fluxi MODEL repo yolo

fluxi install

fluxi serve as NODE_NAME

* Erzeugt einen nginx, konfiguriert ihn, holt ein CERTBOT-Zertifikat

fluxi deploy to NODE_NAME

fluxi readytowork

* Startet alle IDEs und Server fürs Entwickeln

FluxiHints